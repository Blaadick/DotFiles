(defwidget date-time []
    (label
        :text "${formattime(EWW_TIME, "%H:%M:%S")}\\n${formattime(EWW_TIME, "%Y.%m.%d")}"
        :justify "right"
    )
)


(defwidget right []
    (box
        :halign "end"
        :width 100
        (date-time)
    )
)
