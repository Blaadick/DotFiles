(defwidget date-time []
    (label
        :text "${formattime(EWW_TIME, "%I:%M:%S")}\\n${formattime(EWW_TIME, "%Y.%m.%d")}"
        :justify "right"
    )
)


(defwidget right []
    (box
        :halign "end"
        :width 100
        (date-time)
    )
)
